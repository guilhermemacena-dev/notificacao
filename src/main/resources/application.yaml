spring:
  application:
    name: notificacao

  mail:
    host: smtp.gmail.com
    port: 587
    username: guilherme.dev0284@gmail.com
    password: ccum nfrx yzii zfhf
    protocol: smtp
    properties:
      mail:
        smtp:
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  thymeleaf:
    enabled: true

envio:
  email:
    remetente: guilherme.dev0284@gmail.com
    nomeRemetente: 'Guilherme'

server:
  port: 8082